export type City = {
  name: string;
  image: string;
};

export type Destination = {
  name: string;
  location: string;
  image: string;
  price: number;
};

export type Testimonial = {
  name: string;
  quote: string;
};